module.exports = function (activity) {
  return  activity === "push ups"         ||
  				activity === "running"          ||
  				activity === "walking"          ||
  				activity === "climbing stairs"  ||
  				activity === "bicycling"        ||
  				activity === "dips"             ||
  				activity === "crunches"         
}